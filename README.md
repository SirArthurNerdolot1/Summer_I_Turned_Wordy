# Summer I Turned Wordy

A small Vite + React + Tailwind starter for ELS — a daily games portal.

Setup:

```bash
npm install
npm run dev
```

Notes:
- Tailwind is configured via `tailwind.config.cjs`.
- Icons use `lucide-react`.
- Run the dev server then open the app in your browser.
